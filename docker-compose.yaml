services:
  mysqldb:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: my_db
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3307:3306"
    volumes:
      - db:/data/var/lib/mysql

volumes:
  db:
